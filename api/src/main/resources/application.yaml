spring:
  application:
    name: ttt-api
  security:
    oauth2:
      client:
        registration:
          github:
            client-id: ${GITHUB_CLIENT_ID}
            client-secret: ${GITHUB_CLIENT_SECRET}
  data:
    mongodb:
      database: ttt
#logging:
#  level:
#    org.springframework:
#      web: debug
#      security: debug

server:
  error:
    include-message: always